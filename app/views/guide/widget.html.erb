<div class="homepage-col-left">

  <% render :layout=>'widget-neutral', :locals=>{:title=>'widget-neutral'} do %>
    These are mostly used on the homepage (see <a href="homepage_guide.html">Homepage Guide</a>) as a fairly netural container.
  <% end %>

  <% render :layout=>'widget-neutral', :locals=>{:title=>'widget-neutral'} do %>
    <h3 class="font-big color-beige2 dotted-underline">Subheading</h3>
    Inner text.  Inner text.  Inner text.  Inner text.  Inner text.  
    Inner text.  Inner text.  Inner text.  <a href="">link text</a> <br />
    <br />
    <h3 class="font-big color-blue-gray5 dotted-underline">Subheading (Alt. Color)</h3>
    Inner text.  Inner text.  Inner text.  Inner text.  Inner text.  
    Inner text.  Inner text.  Inner text.  <a href="">link text</a> <br />
    <br />
    <h3 class="font-big color-green2 dotted-underline">Subheading (2nd Alt. Color)</h3>
    Inner text.  Inner text.  Inner text.  Inner text.  Inner text.  
    Inner text.  Inner text.  Inner text.  <a href="">link text</a> <br />
  <% end %>

  <% render :layout=>'widget-colored-primary', 
            :locals=>{:title=>"widget-colored-primary", 
                      :rightlink=>{:href=>"", :label=>"Right side link"} } do %>
    <% render :layout=>'widget-colored-primary-inner' do %>
      These are used on the search page (see <a href="search_guide.html">Search Guide</a>), in the left column.<br /><br />Green stands out somewhat more than beige/gray, so this is the container for primary content.
    <% end %>
  <% end %>

  <% render :layout=>'widget-colored-secondary', 
            :locals=>{:title=>"widget-colored-secondary", 
                      :rightlink=>{:href=>"", :label=>"Right side link"} } do %>
    <% render :layout=>'widget-colored-secondary-inner' do %>
      These are used on the search page (see <a href="search_guide.html">Search Guide</a>), in the right column.<br /><br />Beige/gray is subtler than green, so this is the container for secondary content.
    <% end %>
  <% end %>

  <% render :layout=>'widget-colored-secondary', 
            :locals=>{:title=>"widget-colored-secondary" } do %>
    <p>Outer text.  Outer text.  Outer text.  Outer text.  Outer text.  Outer text.</p>
    <% render :layout=>'widget-colored-secondary-inner' do %>
      Inner text.  Inner text.  Inner text.  Inner text.  Inner text.  
      Inner text.  Inner text.  Inner text.  <a href="">link text</a> <br />
    <% end %>
  <% end %>
</div>

<div class="homepage-col-right">
</div>

